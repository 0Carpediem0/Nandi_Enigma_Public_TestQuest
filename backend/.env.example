# Скопируйте в .env и подставьте свои значения.
# В Docker эти переменные задают через docker-compose или -e при запуске.
# Файл .env в репозиторий не коммитить (пароль!).

# Mail.ru
EMAIL_USER=yegor.starkov.06@mail.ru
# Если отправка не идёт: создай «Пароль для внешних приложений»
# (Настройки → Безопасность → Пароли для внешних приложений) и вставь его сюда.
EMAIL_PASSWORD=your_password

# Серверы Mail.ru (отправка пробует порты 465 и 587)
IMAP_HOST=imap.mail.ru
IMAP_PORT=993
SMTP_HOST=smtp.mail.ru
SMTP_PORT=587
